import React from "react";
import AntDesign from "react-native-vector-icons/AntDesign";

const NativeTabBarIcon = ({ name, color }) => {
  return <AntDesign name={name} color={color} size={24} />;
};

export default NativeTabBarIcon;
